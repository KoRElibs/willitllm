# Claude Code local config (machine-specific overrides)
.claude/settings.local.json

# Python virtual environments
.venv/
venv/
env/
.env/

# Python cache
__pycache__/
*.pyc

# Deploy credentials (never commit)
meta/deploy/deploy.cfg

# Cached raw model metadata from ollama /api/show (local working set, not source of truth)
meta/cache/
